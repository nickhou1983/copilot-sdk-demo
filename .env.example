# Copilot SDK Demo 环境变量配置

# ========== 运行模式 ==========

# Server 模式：连接到外部已运行的 Copilot CLI 服务器
# 设置此变量后，SDK 不会自动启动 CLI 进程，而是连接到指定地址
# 格式: "localhost:8080" 或 "http://127.0.0.1:9000" 或只是端口 "8080"
# COPILOT_CLI_URL=localhost:8080

# ========== CLI 配置 ==========

# 自定义 CLI 可执行文件路径（默认从 PATH 中查找 "copilot"）
# COPILOT_CLI_PATH=/usr/local/bin/copilot

# 日志级别: "none" | "error" | "warning" | "info" | "debug" | "all"
# COPILOT_LOG_LEVEL=info

# ========== 服务配置 ==========

# 服务端口（默认 3000）
# PORT=3000
